using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public enum AnimState {
    Idle = 0,
    Walking = 1,
    Running = 2,
    Jumping = 3,
    Falling = 4,
    Guard = 5,
    Hit = 6,
    Airborne = 7,
    Taunting = 10
}